# portfolio-screener — moved

The screen view lives in the tournament repository since 16 September 2026 (Execution Order of 16 September 2026, Phase 6): code under `portfolio-tournament/scripts/screen/`, served data under `portfolio-tournament/data/screen/` (the visibility registry with every provenance field, the vintages), the page at https://wernerhl.github.io/portfolio-tournament/screen.html.

`index.html` here is a redirect to that page. The nightly workflow, the copied stylesheet, chart module and fonts were removed with the move; `scripts/` and `data/` stay as history. This repository is archived read-only after one week of the redirect serving (from 2026-09-23).
